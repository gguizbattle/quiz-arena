[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm --prefix backend ci"]

[phases.build]
cmds = ["npm --prefix backend run build"]

[start]
cmd = "npm --prefix backend run start:prod"
